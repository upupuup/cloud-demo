server:
  port: 8888
spring:
  application:
    name: configserver
  cloud:
    config:
     server:
      git:
        uri: https://github.com/upupuup/cloud-demo.git
        search-paths: config
        username: upupuup
        password: zh15859340868

eureka:
  client:
    serverUrl:
      defaultZone: http://localhost:8761/eureka/
